.env
__pycache__/
*.pyc
api_keys.env

# Rulebook PDFs are copyrighted, not committed (see CLAUDE.md)
rulebooks/

# Local dev / Claude Code state
.claude/

# Debug artifacts
.geekitems_sample.json

# Backup files
*.bak

# Groundedness test outputs (large per-question records — regenerable)
groundedness_results.json
groundedness_results.json.tmp
groundedness_results.json.prev
groundedness_results_baseline.json
groundedness_results_failures_v2.json
groundedness_results_failures_critique_only.json
groundedness_results_v2.json
groundedness_sanity_v2.json
groundedness_sanity_rerank_only.json
groundedness_sanity_critique_only.json
groundedness_summary.csv
groundedness_summary_baseline.csv

# Rulebook gather logs (regenerable)
gather_rulebooks_log.json

# DB backups from local maintenance passes (large, regenerable)
backups/

# Rulebooks removed by sanity checks (holding area, not for git)
rulebooks_removed/

# Scratch PDF page renders from ad-hoc quality checks
scratch_*.png

# One-shot maintenance / sanity logs and their caches
*.log
canonical_audit.json
faq_errata_cache.json
rulebook_sanity_cache.json
bulk_gather_log.json
bulk_gather_extras_log.json
audit_sample.json
golden_results.json
golden_summary.csv

# Golden Q&A dataset (dev-only reference data, paused)
golden_set/

# Raw designer package (only the picks in assets/brand ship)
brand_assets/

